---
heist: code-guessing-startup
phase: the-hit
work-package: WP-016
status: accepted
date: 2026-07-13
territory: H-003
---

# Report: Provenance Public Projection

- Initial RED: the exact command passed the workspace boundary check and failed because the provenance projection module did not exist.
- Rejected first implementation: review rejected a supplied-authorization API that could be paired with unrelated scope, exposed unapproved provider/model/date and other raw source facts, covered only model output, and omitted server-only, recursive immutability, replay and exact rights-item proof.
- Strict rework evidence: actual-authority API RED failed 14/14; scope/content RED reached 19 passed/2 failed; Stack Overflow determination coverage RED reached 21 passed/4 failed; server-entry/frozen-boundary RED reached 26 passed/4 failed; recursive-freeze/first-display RED reached 31 passed/8 failed.
- Final authority boundary: the projector invokes an actual `RevealAuthority` with the request and guards. Premature, replayed, denied and seven-field cross-scope requests return only the pre-reveal candidate projection; a lookalike authority fails closed.
- Evidence and rights boundary: parsed source-specific evidence, accepted provenance publication eligibility and the WP-015 regime must be recursively immutable and bind content, evidence, source class, candidate, disclosure and versions. Stack Overflow additionally requires exact post/revision/license/version determination coverage and one of the two recorded negative first-display decision forms.
- Public output: only the recorded source candidate, approved attribution, copied evidence reference, correctness, detached helpful/misleading signals and content/candidate/scoring/rules/evidence/reveal/source-regime versions leave the boundary. Raw provider, model, URL, author, prompt, output, commission, authorization and internal scope facts remain excluded.
- Packaging: the public entry is marked `server-only`; production has no direct bypass import. Unknown/extra envelope, request and guard fields fail closed, and every returned nested object/array is detached and frozen.
- Fresh root verification: focused command passes TAP 1/1 and 39/39; workspace Vitest passes 659/659; every workspace typecheck passes; final-source production build passes with four static pages.
- Independent Capo verification: focused 39/39, game aggregate 319/319, dependency composition 300/300, game/domain/content typechecks and a coordinated clean production build pass.
- Audits and cleanup: restricted-production-field, prohibited-claim, country/project, workflow-identifier, direct-entry-bypass, static public-bundle and diff audits are clean. Protected `apps/game/tsconfig.json` is restored to SHA-256 `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`; no `.next`, test-results, Playwright report or duplicate-name artifact remains.
- Budget: approximately 14,850 used versus 4,000 planned, within the approved 15,000 ceiling; 150 tokens are returned.

## Reopened Interface Regression

The WP-017 dependency audit found that the accepted implementation compared the correct provenance candidate ID with H-002's `acceptedAnswerId`. The shared round model proves that `answerId` is the immutable accepted-transition identity and `candidateId` is a separate participant selection. The focused fixtures had reused values such as `answer-model` for both concepts and therefore masked the defect. WP-016 is reopened for a strict regression proving that a distinct accepted-transition ID and an incorrect selected candidate still authorize a reveal of the evidence-backed correct recorded source; no completion claim remains active until that RED/GREEN and fresh sweep pass.

The reopened strict RED reached 34 passed/6 failed, all at the invalid candidate/transition equality. The minimal fix removed only that equality and its dead parameter; the authority audit continues to bind `acceptedAnswerId` to the request while the correct source remains evidence/regime-derived. Final focused tests pass 40/40 and prove both authorized correct and incorrect outcomes reveal the recorded correct source, while a cross-transition request returns only PRE_REVEAL. Root passes 660/660 workspace tests, all workspace typechecks and a clean four-page production build; Capo independently passes 40/40 focused, 320/320 game, 300/300 dependency composition, three typechecks and a clean build. Final cumulative usage is approximately 16,850 within the 17,000 ceiling, returning 150 tokens.
