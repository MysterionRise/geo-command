---
heist: code-guessing-startup
phase: the-hit
work-package: WP-017
status: accepted
date: 2026-07-13
territory: H-003
---

# Report: Provenance Round, Clues and Reveal

- TDD chronology: the initial exact command failed on the missing flow module. Expanded acceptance tests remained RED before production, and the rejected-source rework reached 20 passed/15 failed before the shared preflight, exact-envelope and real outcome-state implementation.
- Review corrections: early GREENs were rejected for an underspecified matrix, masked round/rules/regime cases, public factory bypass, preflight-free excerpt exposure, mutable accepted boundaries, permissive nested envelopes and discarded `RoundPlay.acceptAnswer()` state.
- Compiler correction: the final runtime GREEN exposed TypeScript TS2673 because a module-level factory called a private constructor. Interrogation isolated factory placement as the cause. A class-scoped static factory and explicit pre-reveal/authorized-outcome union replaced the invalid construction and `any` return without broadening behavior.
- Accepted behavior: public construction requires the shared WP-016 evidence, publication-eligibility and source-regime preflight. The immutable public round contains the inert excerpt, honest candidates, exactly two ordered progressive clues and all required versions. Answer transition and selected candidate remain distinct.
- Reveal boundary: premature, denied, cross-transition, wrong-round, wrong-regime and version-drift attempts fail closed. Authorized correctness must agree with the selected candidate and evidence-derived correct source. The server outcome retains the immutable answered round/result; JSON and `publicProjection` expose only the approved WP-016 reveal fields.
- Root evidence: exact focused verification passes TAP 1/1 and 35/35; exact named Node Playwright integration passes 1/1; workspace Vitest passes 695/695; every workspace typecheck passes; the production build compiles and generates 4/4 pages.
- Independent Capo evidence: focused 35/35, game 355/355, dependency composition 345/345, full workspace 695/695, relevant typechecks, exact named Playwright 1/1 and an independent four-page build all pass.
- Audits: public static assets contain no answer, evidence, candidate or restricted-schema markers and no source maps. Workflow-ID, prohibited-claim/mode, restricted-field, unsafe-type, fixture-authority, direct-route/import and whitespace audits are clean. No route or excluded mode was activated.
- Cleanup: `apps/game/tsconfig.json` is restored to SHA-256 `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`; no `.next`, test-results or Playwright-report artifact remains.
- Budget: approximately 20,900 used versus 4,000 planned, within the approved 21,000 ceiling; approximately 100 tokens are returned.
