---
heist: code-guessing-startup
phase: the-hit
work-package: WP-018
status: accepted
date: 2026-07-14
territory: H-003
---

# Report: Provenance Calibration and Claims Audit

- Scope decision: H-003 Capo required one small flow-owned calibration module because the accepted provenance flow had no immutable calibration record. The released production scope was limited to that module, flow integration and server export; authoritative event production, rights regimes, formulas, cross-mode claims and content redesign remained closed.
- Drill evidence: the initial populated RED passed the claims suite but exposed one test-only duplicate-pattern expectation, which Interrogation corrected. H-003 Capo then returned the clean missing-module RED for four test-only gaps. The completed RED passed 20/20 claims tests and failed solely because `provenance-calibration.ts` did not exist before production began.
- Calibration boundary: the factory accepts exactly `versionId`, `sourceRegimeVersionId`, `presentedCandidateCount`, `chanceBaseline`, `clueSetVersionId`, `configuredClueCount` and `scoringVersionId`. It rejects missing, extra, null, undefined, blank, mutable, count-drift and epsilon-approximate inputs, reparses factory output, detaches the record and recursively freezes it.
- Flow binding: flow creation reparses calibration and binds source-regime version, the two real candidates, exact `1 / presentedCandidateCount`, actual clue-set version, two configured clues and actual scoring version. The public round adds only calibration and evidence version identities; chance, count, correct source, correctness and source class remain absent before authorized reveal.
- Event proof: real flow outcomes cover zero, one and two accepted clues for each of the two candidate selections. Each exact WP-028 `ANSWER_ACCEPTED` event binds the opaque accepted transition, actual acceptance time, request scope, regime version, selected candidate independently of correctness, count two, actual clue count, provenance mode and scoring version. Family/schema and every scope/value drift reject; parser output is detached, reparsable and recursively frozen; private and inapplicable fields reject including null placeholders.
- Claims boundary: the audit recursively enumerates a non-vacuous project-controlled communication source set while excluding Gangsta evidence, tests, fixtures, generated output and reports. TypeScript/TSX strings, templates and JSX text are extracted through the TypeScript AST; Markdown prose excludes fenced code. NFKC-normalized contextual rules reject AI detection/classification, authorship/identity, code-quality claims, equal ability, statistical causality, guaranteed displayed-code recall/removal, acquisition, monetization, market-size and public-retention claims while permitting the exact recorded human, model and Stack Overflow labels plus entertainment-only chance-aware copy.
- Review: the independent Gangsta Inspector reproduced TAP 1/1 plus 102/102 focused tests and direct strict compilation, audited the signed H-003 boundary, privacy surface and claim scanner, and returned CLOSED with no finding or edit.
- Verification: `pnpm test -- provenance-flow provenance-calibration claims-audit` passes TAP 1/1 plus 102/102. Direct strict compilation of the three reviewed tests and fixture exits zero. The full workspace passes TAP 1/1 plus 1,333/1,333 tests and all five typechecks. `pnpm test:e2e -- --project=provenance` passes 1/1 against the real managed local server.
- Cleanup: the E2E-created Next configuration drift was restored to canonical SHA-256 `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`, followed by a clean game typecheck. No `.next`, `test-results`, Playwright report or port-3000 listener remains; diff whitespace checks are clean.
- Budget: approximately 17,800 used versus 4,000 planned, within the approved 18,000 ceiling; approximately 200 tokens are returned. Operational programme reforecast: 531,550 tokens.
