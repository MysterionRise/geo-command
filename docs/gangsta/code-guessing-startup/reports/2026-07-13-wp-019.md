---
heist: code-guessing-startup
phase: the-hit
work-package: WP-019
status: accepted
date: 2026-07-13
territory: H-004
---

# Report: Language Candidate Sets and Aliases

- Release gate: signed H-001, H-002 and H-004 artifacts mechanically match their registered SHA-256 hashes and all package prerequisites are accepted.
- TDD chronology: the acceptance-complete suite failed solely at the missing language module before production existed. Root and Capo rejected two earlier tests-only matrices until clue bounds, nested exactness/copying, variable candidate counts, calibration binding, ordering policy/record identity and malformed cases were complete.
- First GREEN review: 77/77 focused tests and domain typecheck passed, but a runtime probe proved both exported consumers accepted mutable and deep-frozen structural lookalikes that bypassed the factory. Interrogation isolated the defect to uncertified structural interfaces.
- Certification RED/GREEN: the strict regression reached 78 passed/4 failed across mutable/frozen lookalikes for alias resolution and presentation, while a factory-created control passed. A module-private `WeakSet` now certifies the exact immutable factory result; both consumers fail closed on clones and lookalikes. Final focused tests pass 82/82.
- Candidate semantics: the factory validates exact recursively frozen input, copies and recursively freezes output, normalizes NFKC labels/aliases globally, rejects collisions, binds one correct candidate, requires nonblank rationale only for distractors, retains exact presented count, and supports a computed `1 / count` chance baseline including three-candidate `1 / 3` proof.
- Clue/calibration semantics: zero through two consecutively ordered unique clue references are versioned, and calibration exactly binds candidate-set version, presented count, chance baseline, clue policy/count and scoring version.
- Ordering semantics: deterministic and explicitly recorded randomized policies require immutable exact-key records, a bound session identifier, exact candidate permutation, policy/set versions and randomized record/time evidence where applicable.
- Verification: Root and Capo independently pass 82/82 focused, 186/186 domain aggregate, TAP 1/1 plus 777/777 workspace tests and all five workspace typechecks. Contract hashes and workflow-ID, prohibited-mode/claim, unsafe-operation, whitespace and artifact audits are clean.
- Carry-forward: WP-021 must compare the ordering record session to the authoritative bound session. NFR-007 bidi/zero-width approval/annotation belongs to the language eligibility/flow boundary because the parent permits reviewed exceptions.
- Budget: approximately 19,150 used versus 4,000 planned, within the approved 21,000 ceiling; approximately 1,850 tokens are returned.
