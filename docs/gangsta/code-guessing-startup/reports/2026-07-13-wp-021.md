---
heist: code-guessing-startup
phase: the-hit
work-package: WP-021
status: accepted
date: 2026-07-13
territory: H-004
---

# Report: Language Round, Clues and Reveal

- TDD chronology: the acceptance-complete focused matrix preceded production and failed solely because the language server module was absent. It binds the manifest-supplied round version, exact evidence and eligibility, certified candidates and ordering, authoritative session, zero-to-two clues, opaque answer transition, reveal envelopes, version drift, public/private state and deceptive-control disposition.
- Resource controls: two implementation workers were interrupted and conservatively charged after repeated no-artifact/no-status checkpoints. Root then implemented only the accepted language server flow and exact `server-only` entry. No test, route or accepted dependency was weakened to obtain GREEN.
- Accepted composition: construction requires a recursively frozen, exactly reparsed EvidenceRecord; a WP-019 factory-certified candidate set and reconstructed per-session presentation; a complete WP-020 ambiguity/control eligibility reconstruction; a manifest-supplied round-version binding; and an actual pristine RoundPlay. The public round preserves the recorded candidate order, count, zero-to-two ordered clues and every required version without exposing the answer, distractor rationales or restricted evidence.
- Answer and reveal: one frozen opaque transition accepts only a presented candidate and locks later clues/answers. Premature, denied and cross-scope requests return the exact minimal PRE_REVEAL projection. Authorized reveals validate the actual RevealAuthority audit, approved attribution/evidence, six payload versions and evidence-derived correctness before retaining a real immutable answered RoundPlay/result server-side. JSON emits only the recursively frozen approved public projection.
- Inert controls: actual excerpt characters are classified against the independently reviewed WP-020 disposition. Absent review rejects bidi and zero-width controls; approved display requires exact reviewed classes, annotation version and project-controlled version-to-content binding. Strict Inspector regressions prove altered nonblank annotation text fails and U+2061/U+2064 invisible operators cannot pass an absent review.
- Drill evidence: focused runtime first reached 86/86. TypeScript then exposed nine control-flow narrowing errors; Interrogation proved the single cause with a one-site experiment and explicit value-producing guards restored compiler GREEN without behavior change. Inspector rework separately witnessed 86 passed/1 failed before annotation-content GREEN, then 87 passed/2 failed before the invisible-control GREEN. Final focused verification passes TAP 1/1 plus 89/89.
- Integration evidence: the named Node Playwright spec preceded its configuration and failed solely because project `language` was absent. One exact project entry reaches 3/3 against the real flow, RevealAuthority and RoundPlay. Real browser, viewport, keyboard and assistive-technology language-flow evidence remains assigned to WP-036 by the signed Execution Plan and is not claimed here.
- Final verification: Root passes TAP 1/1 plus 933/933 workspace tests, all five workspace typechecks, named language E2E 3/3 and a production build that compiles, typechecks and generates 4/4 pages. H-004 Capo and the independent Inspector reaccept the final NFR-007 corrections with no unresolved finding.
- Audits and cleanup: parent/child hashes, workflow identifiers, unsafe execution/network, excluded modes, routes/analytics, public containment, whitespace and generated artifacts are clean. The supported entry is marked server-only; direct implementation imports are confined to tests. `apps/game/tsconfig.json` is restored to SHA-256 `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`; no `.next`, test-results, Playwright report or port-3000 listener remains.
- Budget: approximately 93,500 used versus 4,000 planned, within the approved 95,000 ceiling; approximately 1,500 tokens are returned. Operational programme reforecast: 446,550 tokens.
