---
heist: code-guessing-startup
phase: the-hit
work-package: WP-014
status: implementation-accepted-operational-evidence-indeterminate
date: 2026-07-13
territory: H-002
---

# Report: Accessibility, Browser and Performance Boundary

- Initial RED: the exact accessibility command was missing; after test-only scripts were introduced, accessibility failed 0/3 and performance failed 0/5 because the truth-gate modules did not exist.
- Branch-complete RED/GREEN added structured accessibility dimensions and PASS/FAIL/INDETERMINATE behavior, strict performance/sample/idempotency/freshness/availability evaluation, and malformed numeric rejection. Synthetic fixtures are explicitly labeled and do not count as operational evidence.
- Browser RED: the first exact chain reached 2/3 E2E and exposed that the served no-JavaScript document did not present its explanation. The minimal fix is ordinary server-rendered explanatory text; it adds no partial gameplay, fake reveal action, or public answer authority.
- Harness-ownership RED: the pnpm-wrapped Next web server left an orphan listener that regenerated build output and protected configuration drift. The final Playwright configuration directly owns the pinned local Next process with a scoped application working directory.
- Final technical GREEN: exact `pnpm test:a11y && pnpm test:e2e && pnpm test:performance` passes 5/5, 3/3 and 6/6. Game tests pass 280/280, workspace Vitest passes 587/587 plus TAP 1/1, runner passes 1/1, game typecheck and production static build pass.
- Accessibility evidence: frozen browser/AT/viewport/no-JavaScript targets and structured truth gate at `tests/accessibility/support-gate.mjs:1`; branch proof at `tests/accessibility/support-gate.test.mjs:13`; local-only minimum-viewport/no-JavaScript proof at `tests/e2e/accessibility-local.spec.ts:3`.
- Performance evidence: exact load, p95, excerpt, reference-client, sample, freshness, availability and idempotency targets at `tests/performance/performance-gate.mjs:1`; sufficiency, miss and malformed-evidence proof at `tests/performance/performance-gate.test.mjs:13`.
- Independent in-app browser evidence: the live server rendered one main landmark, the expected heading and JavaScript explanation at exactly 320 by 568 CSS pixels; document scroll width was exactly 320, no interactive controls were present, and browser error/warning logs were empty.
- Cleanup/safety evidence: no TCP 3000 listener, `.next`, `test-results`, `playwright-report`, duplicate generated file, country-mode activation, workflow identifier or public answer authority remains. Final `apps/game/tsconfig.json` SHA-256 is `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`, followed by clean typecheck and diff checks.
- Operational decision: implementation is accepted, but current major browser versions, required AT combinations, complete mode flows, fifteen-minute load, per-endpoint 1,000 samples, per-family 100 client samples and active-beta availability evidence remain uncollected. The gate is `INDETERMINATE`, invitations remain blocked, and no operational PASS is claimed.
- Budget: approximately 14,600 used versus 5,000 planned, within the approved 14,700 ceiling; 100 tokens are returned.
