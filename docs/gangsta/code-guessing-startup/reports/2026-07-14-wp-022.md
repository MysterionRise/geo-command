---
heist: code-guessing-startup
phase: the-hit
work-package: WP-022
status: accepted
date: 2026-07-14
territory: H-004
---

# Report: Language Calibration and Ordering Audit

- Scope decision: H-004 Capo restricted WP-022 to the two audit files named by the Execution Plan. WP-019 already enforces candidate/calibration and ordering factories, WP-021 owns the real language flow, and WP-028 owns authoritative events; no duplicate production module, randomizer, formula engine or cross-mode claim was added.
- Certified reconstruction: both three- and four-candidate sets are factory-reparsed, certified through opaque `lang-*` IDs that cannot equal normalized labels, and rebuilt into detached presentations. Candidate arrays/items/aliases, policies, clues, calibration, presentation IDs, ordering records and presented-ID arrays are separately detached and recursively frozen.
- Chance and clue calibration: exact `1 / presentedCandidateCount` baselines prove both terminating and non-terminating cases without rounding. Candidate-set, presented-count, clue-policy, configured-clue-count, scoring and calibration versions reject every isolated drift. Real WP-021 flows exercise configured and accepted clue counts zero, one and two through actual RoundPlay outcomes.
- Ordering audit: deterministic policy preserves declaration order and authoritative session binding. Randomized policy accepts only an explicit exact permutation with candidate-set/policy/session scope, record identity, randomization identity and recorded time. Wrong scope/version/kind, reordered deterministic IDs, duplicates, unknowns, missing/blank evidence, extra fields and mutable roots/nested arrays reject.
- Event mapping: each real answered flow produces an exact recursively frozen WP-028 `ANSWER_ACCEPTED` input whose event ID equals the opaque accepted-answer transition, acceptance time equals the immutable answer time, scope comes from the authoritative request, and candidate/set/count/clues/mode/scoring facts come from the certified flow. Both correct and incorrect selections map their actual candidate while correctness remains absent until reveal.
- Absence and containment: correctness, chance/calibration/order facts, correct answer, labels, aliases, rationales, clue payload/text, answer/reveal/evidence payloads, prompt, code, extras and null placeholders reject. The pre-reveal round remains spoiler-free while retaining required evidence/calibration/ordering version identifiers.
- Review rework: the first populated run passed 64/65; Interrogation found a test-only false positive caused by banning the required evidence-version key. The independent Inspector then returned strict-compile, opaque-ID, nested-detachment and test-organization findings. All were accepted under Receiving Orders and independently re-audited CLOSED with no remaining finding.
- Final verification: `pnpm test -- language-calibration candidate-ordering` passes TAP 1/1 plus 70/70. Direct strict compilation of both files passes with project-equivalent first-party flags and third-party declaration checking isolated. Game, domain and measurement typechecks pass; the full workspace passes TAP 1/1 plus 1,266/1,266 and all five typechecks.
- Budget: approximately 11,200 used versus 4,000 planned, within the approved 12,000 tests-only ceiling; approximately 800 tokens are returned. Operational programme reforecast: 517,750 tokens.
