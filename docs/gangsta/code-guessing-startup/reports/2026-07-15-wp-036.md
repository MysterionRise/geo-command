---
heist: code-guessing-startup
phase: the-hit
work-package: WP-036
status: technical-green-operational-evidence-indeterminate
date: 2026-07-15
territory: H-004
---

# Report: Language Accessibility Evidence

- Scope: WP-036 adds a language-specific accessibility evidence evaluator, exact mode-runner selection and a named local Chromium project. It changes no product source. The evaluator covers keyboard, focus, announcements, screen-reader labels, contrast, non-color state, text equivalents, responsiveness, reduced motion, no-JavaScript containment, candidate presentation, inert-control disclosure, reveal containment, correction, error recovery and the prohibition on cross-mode comparability claims.
- Complete-session boundary: every operational browser/version and assistive-technology record must contain exactly two distinct completed five-round sessions and exactly two uniquely identified, ascending language rounds in each session. Those four language rounds must collectively cover zero, one and two clues, both candidate outcomes and `ABSENT`, `BIDI`, `ZERO_WIDTH` and `BIDI_AND_ZERO_WIDTH` controls. Every round must contain the exact ordered base-excerpt, two-clue, answer, reveal, correction and error stages with every required check.
- Candidate and control boundary: each round binds exact candidate-set, ordering-record and ordering-policy versions to the containing session; candidate identities, count, selected identity and measured DOM, focus and screen-reader order must equal the certified presentation. Inert controls require the exact approved WP-021 annotation version, text and detected classes plus inert escaped rendering, visible non-color disclosure and screen-reader equivalence. `ABSENT` must remain unannotated.
- Truth boundary: only authenticated `OPERATIONAL_MEASURED` evidence in the retained trust domain can PASS. Synthetic, local, unverified, cross-domain, missing, duplicate, malformed, unordered or structurally inexact evidence is INDETERMINATE. Any authenticated measured required failure takes precedence over unrelated incompleteness and produces FAIL. Malformed top-level rows, sessions, rounds, stages and nested records fail safely without exceptions.
- Local evidence: bundled Chromium proves only the locally observable minimum-viewport, reduced-motion, overflow and no-JavaScript boundaries. It explicitly records that the current static page lacks the complete language, control-review, correction and error flows. `LOCAL_AUTOMATED` evidence cannot satisfy a browser family, assistive-technology combination or reviewer approval.
- Audit history: the Capo rejected positive-only RED coverage until the matrix independently exercised every exact session, candidate, language-semantic, inert-control, browser, AT, no-JavaScript, review and authority boundary. After initial GREEN, the Capo found null browser/AT row and nested malformed language-round exception paths. Both were witnessed RED at 17/18 before minimal guards restored 18/18. Capo then reported ACCEPT. The independent Inspector reports CLOSED with no Critical or Important finding and confirms the Node executable pin and 120-second startup timeout do not weaken test semantics.
- Operational decision: AC-012 remains HOLD/INDETERMINATE. No current latest-two-major browser matrix, physical iOS/iPadOS or Android run, VoiceOver/NVDA run, complete deployed language/control/correction/error flow or qualified Accessibility Reviewer approval exists. Invitations remain blocked and no operational PASS is claimed.
- Verification: `pnpm test:a11y -- --mode=language` passes 18/18. `pnpm test:e2e -- --project=language-accessibility` passes 2/2 against the managed local server. The final workspace sweep passes TAP 1/1 plus 1,527/1,527 Vitest tests and all five typechecks; `git diff --check` is clean. The protected game config is restored to SHA-256 `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`; no generated artifact or listener remains.
- Budget: approximately 20,000 used versus 4,000 planned, within the 21,000 cumulative ceiling; approximately 1,000 tokens are returned. Operational programme reforecast: 910,050 tokens.
