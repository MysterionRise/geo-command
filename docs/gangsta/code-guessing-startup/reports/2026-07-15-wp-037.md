---
heist: code-guessing-startup
phase: the-hit
work-package: WP-037
status: technical-green-operational-evidence-indeterminate
date: 2026-07-15
territory: H-005
---

# Report: Enrollment and Withdrawal Accessibility Evidence

- Scope: WP-037 adds a privacy-flow accessibility evidence evaluator, exact `--flow=privacy` runner selection and a named local Chromium project. It changes no product, identity, consent, withdrawal, provider or operations source. The evidence boundary covers enrollment credentials, eligibility, consent, session completion, correction notices, authenticated withdrawal, deletion status and recoverable errors.
- Complete-journey boundary: every operational browser/version and assistive-technology record must contain exactly two distinct identity-bound completed five-round journeys. Each journey records the exact ordered thirteen privacy stages and all nine required accessibility checks. Together the two journeys cover `VOID` and `CONTENT_WITHDRAWN`, `DELETE` and `IRREVERSIBLY_DELINK`, and the exact five enrollment, consent, correction-acknowledgement, withdrawal-authentication and deletion-status error cases. Each error has exact presented/recovered stages, sensitive-echo absence, no partial mutation, repaired or preserved focus and successful recovery.
- Consent/correction boundary: invitation, enrollment, participant-lineage, session, correction-notice and deletion-case identities are nonblank, mutually bound and distinct between the two journeys. Eligibility and consent remain distinct under the same nonblank policy version, consent begins unselected and withdrawal terms are presented. Correction binds the participant, session, round, notice identity and notice version; both correction states require acknowledgement, adjusted-maximum presentation, non-color and screen-reader evidence, and `CONTENT_WITHDRAWN` remains generic.
- Withdrawal/deletion boundary: withdrawal must be authenticated and expose exactly the seven atomic effects—consent withdrawn, analysis excluded, credential cascade revoked, optional telemetry disabled, optional processing stopped, deletion case opened and audit recorded. Later physical deletion is explicitly `ASYNCHRONOUS` and unfinished work cannot be shown as complete. Deletion status is ordered across case opening, acknowledgement, active stores, derived records, provider propagation and backup ageing, with exact 7/30/35-day deadlines and accessible announcement, non-color and screen-reader state.
- Privacy/truth boundary: evidence recursively rejects raw credentials, authentication proofs, recruitment identity, free text, raw code, prompts, IP fingerprints, full user agents and secrets, including inside arrays and nested records. Only authenticated `OPERATIONAL_MEASURED` evidence in the retained trust domain with independently verified qualified Accessibility Reviewer approval can PASS. Authenticated measured and semantic failures take precedence; synthetic, local, cross-domain, unverified, missing, duplicate, malformed, unordered or structurally inexact evidence is INDETERMINATE. Malformed nested evidence cannot throw.
- Local evidence: bundled Chromium proves only the locally observable minimum viewport, reduced motion, overflow and no-JavaScript explanation-only boundaries. It explicitly records that enrollment, consent, withdrawal, correction, deletion-status and error flows are absent. `LOCAL_AUTOMATED` evidence never feeds the operational evaluator.
- Interrogation and audit: the first named E2E attempt timed out before assertions at the 120-second managed-server readiness boundary. A clean direct pinned-Node reproduction measured 79.5 seconds to Next readiness plus 162 seconds to compile the first route. The managed-server allowance was therefore raised to 300 seconds; retries remain zero, workers one, server reuse disabled and assertions unchanged. The clean exact E2E then passed. Capo reports ACCEPT and independent Inspector reports CLOSED with no Critical or Important finding.
- Operational decision: AC-012 remains HOLD/INDETERMINATE. No current latest-two-major browser matrix, physical iOS/iPadOS or Android run, VoiceOver/NVDA run, complete deployed privacy flow or qualified Accessibility Reviewer approval exists. Invitations remain blocked and no operational PASS is claimed.
- Verification: `pnpm test:a11y -- --flow=privacy` passes 21/21. `pnpm test:e2e -- --project=privacy-accessibility` passes 2/2 against the managed local server. The final workspace sweep passes TAP 1/1 plus 1,527/1,527 Vitest tests and all five typechecks; `git diff --check` is clean. The protected game config is restored to SHA-256 `d0764b823179d444055a666be0fd4adc45e948cbb71c0f29efa6a3a0b2487a76`; no generated artifact or listener remains.
- Budget: approximately 26,000 used versus 4,000 planned, adding 22,000 to the operational programme reforecast for 932,050 tokens.
