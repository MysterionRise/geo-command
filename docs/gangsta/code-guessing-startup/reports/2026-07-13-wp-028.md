---
heist: code-guessing-startup
phase: the-hit
work-package: WP-028
status: accepted
date: 2026-07-13
territory: H-006
---

# Report: Authoritative Event Schema

- Contract surface: the accepted parser registers exactly the seventeen frozen event families and `authoritative-events-v1`. Each family has the four universal facts plus its exact signed whitelist; participant/day/manifest/session facts are present only where applicable, and missing, null, present-undefined, guessed and cross-family fields fail closed.
- Conditional absence: withdrawal deletion references exist only for `WITHDRAWN`; credential issuance carries neither relationship, reissue requires only its predecessor and revocation requires only distinct descendants; incident `endedAt` is optional but, when present, must be canonical UTC and not precede `startedAt`. All inputs and nested values must already be recursively frozen, while accepted outputs are detached and recursively frozen.
- Value integrity: canonical millisecond UTC and beta-day dates, every frozen enum, ordinal/clue/candidate/completion bounds, exact five-round counts, closed-response identifiers, stable class/treatment/owner identifiers and batch-global event-ID uniqueness are enforced. Reveal denial exposes only its exact non-sensitive scope and rejects every answer, correctness, evidence, reveal, session/content and rules payload probe.
- Privacy boundary: recursive key scanning applies NFKC and lowercase normalization, removes Unicode punctuation, separators, format characters and whitespace, and compares only against the narrow NFR-009 alias set. Raw code, prompts, free text, IP-derived fingerprints, full user agents, secrets/tokens and recruitment identity are blocked without scanning values or overblocking generic metadata keys.
- Type contract: `AuthoritativeEvent` is a parser-only unique-symbol-branded discriminated union for all seventeen families. Universal and family-specific fields narrow by `eventFamilyId`; nested round counts, transitions and identifier arrays are deeply readonly. Compile-time negative proofs reject sibling-family fields, mutation and arbitrary-record construction.
- Drill evidence: the accepted test matrix preceded production and failed solely because `src/events/index.ts` was absent. Initial GREEN reached 258/259; Interrogation isolated one error-message classification and the contextual transition validator restored 259/259 plus strict typecheck. Inspector rework separately witnessed four telemetry-obfuscation runtime failures and seven type-contract compiler failures before reaching final GREEN.
- Independent review: the Inspector's two Important findings—separator/format telemetry evasion and the loose exported record type—were accepted under Receiving Orders, corrected without weakening the matrix, and independently re-audited CLOSED with no remaining finding.
- Final verification: focused measurement tests pass 263/263; measurement typecheck passes; the complete workspace passes TAP 1/1 plus 1,196/1,196 Vitest tests and all five workspace typechecks. No `.next`, `test-results` or Playwright report artifact is present.
- Scope: production changes are confined to `packages/measurement/src/events/index.ts` and the measurement barrel; test/config changes are confined to the authoritative event runtime matrix, compile-time type contract, measurement test script and type-test include. No formula, beta-operation, provider, invitation or external system was invented or activated.
- Budget: approximately 69,000 used versus 5,000 planned, within the approved 75,000 cumulative ceiling; approximately 6,000 tokens are returned. Operational programme reforecast: 510,550 tokens.
