---
heist: code-guessing-startup
phase: the-hit
work-package: WP-020
status: accepted
date: 2026-07-13
territory: H-004
---

# Report: Language Ambiguity Eligibility

- Contract boundary: the accepted artifact composes the actual WP-019 factory-certified candidate set and a detached semantic reconstruction of the existing four-role publication approval. It does not duplicate or alter the accepted publication/domain factories.
- Ambiguity semantics: exactly one technical reviewer A and B must match the publication identities, names, roles, exact qualifications, dates, content/evidence/candidate-set versions and conflict-free status. Their unique versioned review records must each contain only the one certified correct opaque candidate ID; zero, duplicate, second known, unknown, disagreement, agreed-wrong and polyglot decisions reject eligibility.
- NFR-007 decision: the existing independent rights/safety reviewer provides a versioned, dated and item/set-bound control decision. Eligibility permits only no deceptive controls, or detected bidi/zero-width controls with an approved nonblank visible-annotation version. Missing, unapproved, mismatched, duplicate, unknown and unannotated decisions fail closed.
- Publication handling: structurally valid publication records are reconstructed through `createPublicationEligibility`, detached and recursively frozen. Extra, semantically tampered, non-language or mismatched records reject. Mutable nested publication structures can be safely copied inside a frozen envelope; mutable ambiguity/control boundaries reject.
- Drill recovery: the first test harness incorrectly destructured tuple-driven `it.each` cases and its provenance fixture failed upstream. A 57-pass/9-fail run invalidated the apparent GREEN. The premature implementation/export were deleted, all nine callbacks and the fixture were corrected, and a fresh missing-module RED was witnessed before a from-scratch implementation.
- Opaque-ID regression: the fresh implementation reached 66/66, but Capo review found that it passed the correct opaque candidate ID to a label/alias resolver. A strict regression reached 66 passed/1 failed with `lang-ts-01`; the minimal repair certifies through the correct candidate's canonical label and verifies the resolver returns the authoritative opaque ID. Final focused tests pass 67/67.
- Verification: Root and Capo independently pass 67/67 focused, 303/303 content aggregate, 82/82 WP-019 dependency, TAP 1/1 plus 844/844 workspace tests and all five workspace typechecks. Contract hashes, exact-shape, workflow-ID, unsafe-operation, malformed-table, whitespace, config and generated-artifact audits are clean.
- Carry-forward: WP-021 must consume the reviewed control disposition in actual inert rendering and compare the ordering record session ID with the authoritative bound session.
- Budget: approximately 37,500 used versus 4,000 planned, within the approved 39,000 ceiling; approximately 1,500 tokens are returned.
