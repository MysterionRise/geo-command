---
heist: code-guessing-startup
phase: the-hit
work-package: WP-031
status: technical-green-operationally-blocked
date: 2026-07-15
territory: H-006
---

# Report: Incident, Outage and Freshness Operations

- Scope: a provider-neutral operations engine now records signed incidents, evaluates per-minute availability and streak protection, enforces five-minute fresh delivery, and verifies quarantine-backed cache purge. It consumes injected authorities, clocks and ports; it does not invent providers, schedules, production keys or deployment approval.
- Signed boundaries: incident policy and each incident occurrence are independently digest-bound to a stable trust domain. The retained occurrence attestation covers the exact parsed WP-028 event, release operator, decision owner and chronology; recording and every downstream rehydration reverify both envelopes. Cross-domain reuse fails before even a permissive signature verifier can accept it.
- Outage and streak behavior: availability uses a joined incident calendar and exact per-minute 99-percent threshold. Missing evidence is INDETERMINATE. Streak protection is limited to an exact affected scope and overlapping platform outage; it preserves an existing streak but never invents completion.
- Freshness and purge behavior: delivery is denied before any provider port when evidence is stale, future-dated, unverified or scope/version drifted. Purge requires a domain-verified attestation whose digest covers the complete quarantine record, plus matching content/reveal/issuance scope, credential revocation, participant notice and authoritative chronology. `cachePurgedWithinDeadline` must be true, and the module makes no unsupported recall claim.
- Drill and audit history: the initial missing-module RED reached 10/10 GREEN. Capo review reopened structural policy forgery and calendar joins; the corrective RED witnessed 3 failures with 7 preserved passes. Independent Inspector review then reopened unsigned occurrence minting, streak-scope drift, incomplete quarantine/purge proof and unused trust domains. The final accepted security RED witnessed 6 failures with 4 preserved passes before the four controls above reached 10/10. Capo reports GREEN ACCEPTED and the independent Inspector reports CLOSED with no Critical or Important finding.
- Operational boundary: WP-031 is technically GREEN but not operationally accepted. The project-controlled provider schedule remains `BLOCKED_PENDING_DON_PROVIDER_SCHEDULE` with seven unresolved categories and zero provider entries. No provider adapter, live scheduler, route, deployment approval or recruitment authorization was introduced.
- Verification: `pnpm test -- incidents outages revocation-freshness` passes 10/10 and direct strict compilation of the three tests passes. The workspace passes TAP 1/1 plus 1,527/1,527 Vitest tests and all five typechecks; `git diff --check` is clean. Independent audit-review is CLOSED.
- Budget: approximately 38,000 used versus 5,000 planned, within the approved 40,000 cumulative ceiling; approximately 2,000 tokens are returned. Operational programme reforecast: 874,050 tokens.
