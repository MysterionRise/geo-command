---
heist: code-guessing-startup
phase: the-hit
work-package: WP-030
status: accepted
date: 2026-07-14
territory: H-006
---

# Report: UTC Release and Day 7 Gate

- Scope: a provider-neutral release engine now models fourteen half-open active UTC days, per-day grace, the Day 7 freeze/pause, deterministic evidence-floor and comparator outcomes, exact PASS/FAIL/INDETERMINATE recommendation mapping, and a hard Day 8 authorization boundary. Live dates, thresholds, report results and continuation decisions are inputs, not fabricated constants.
- Signed boundaries: lifecycle, gate configuration, Day 7 report, Don continuation decision and any departure rationale are verified against exact canonical normalized envelopes. Authenticated release operators and approval verifiers are injected through a trust-domain-bound authority boundary; only the five intended runtime values are publicly exported.
- Durable authorization: Day 8 opens only at the first UTC midnight strictly after a valid signed OPEN decision. A serialized transactional store provides restart-safe idempotency, canonical replay identity, one sticky accepted transition and later-attempt history without erasing acceptance. Corrupt prior state aborts inside the transaction without overwriting raw bytes.
- Audit integrity: every attempt records authoritative server time, named operator, report/version, decision digest, signature, rationale lineage and the complete signed decision envelope. Each row is provider-neutrally sealed, and the complete normalized authorization state is separately sealed so mutation, insertion, deletion and valid-seal tail truncation fail closed.
- Drill and audit history: the initial missing-module RED reached first GREEN at 79/79. Independent review reopened verified authority, durable terminal state, authoritative time, canonical replay and evidence-lineage gaps; the corrective RED witnessed 11 failures with 73 preserved passes and reached 84/84. A second review reopened public provenance minting, cross-verifier trust-domain confusion and destructive corruption handling; the narrow RED witnessed 7 failures with 80 preserved passes and reached 87/87. A final review found mutable denial reasons and then valid-seal tail truncation. Their accepted REDs witnessed 3/84 and 3/85 respectively before the final state-seal implementation reached 88/88. The independent Inspector now reports CLOSED with no Critical or Important finding.
- Operational boundary: the generic engine is accepted, but live Day 1 dates, the Don-approved threshold package, signed Day 7 report, signed OPEN decision, production authority provider/key material and provider-specific incident inputs remain unconfigured. Day 8 therefore remains operationally blocked; no approval, result or provider fact has been invented.
- Verification: `pnpm exec vitest run apps/game/test/utc-lifecycle.test.ts apps/game/test/day7-gate.test.ts` passes 88/88. Direct strict compilation of both release tests passes. The workspace passes TAP 1/1 plus 1,517/1,517 Vitest tests and all five typechecks; `git diff --check` is clean. Independent audit-review is CLOSED.
- Budget: approximately 162,000 used versus 5,000 planned, within the approved 165,000 cumulative ceiling; approximately 3,000 tokens are returned. Operational programme reforecast: 841,050 tokens.
