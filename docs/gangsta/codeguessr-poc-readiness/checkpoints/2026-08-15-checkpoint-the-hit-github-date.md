---
heist: codeguessr-poc-readiness
phase: the-hit
status: blocked
timestamp: 2026-08-15T13:17:05Z
updated-at: 2026-08-29T12:17:30Z
next-action: Don decides whether to authorize a separate unsupported-status investigation
completed-wps: [WP-001, WP-002, WP-003, WP-004, WP-005, WP-006, WP-007, WP-008, WP-009, WP-010, WP-011, WP-012, WP-013, WP-014, WP-015, WP-016, WP-017, WP-018, WP-019, WP-020, WP-021, WP-022, WP-023, WP-024, WP-026, WP-030, WP-031, WP-032, WP-033, WP-035]
in-progress-wps: []
pending-wps: [WP-025, WP-027, WP-029]
blocked-wps: [WP-028, WP-034]
failed-wps: []
artifacts:
  - docs/gangsta/codeguessr-poc-readiness/specs/2026-08-15-contract-revision-11-signed.md
  - docs/gangsta/codeguessr-poc-readiness/plans/2026-07-31-execution-plan.md
  - docs/gangsta/codeguessr-poc-readiness/evidence/2026-08-14-github-date-live-boundary.md
---

# Revision-11 GitHub Timestamp Hit

The signed revision-11 Contract authorizes one narrow Source Adapter change:
accept and normalize only the exact enumerated numeric-offset committer
timestamps while preserving the raw provider lexeme in canonical response and
crawl-snapshot hashing. WP-035 must show a genuine failing test before any
production edit and must receive independent Crew Lead verification.

WP-034 remains blocked until that acceptance. All query, capacity,
completeness, warning, admission, Stack, exact three/two composition,
localhost-only game, unreviewed-content, network-free gameplay, and atomic
publication boundaries remain closed to change.

Source Adapters accepted WP-035 after a genuine one-test RED on the old
`Z`-only parser. Independent Green is 32/32 focused tests, 329/329 full
preparation tests, and a clean ops TypeScript check. Exact raw responses remain
the response-hash authority before normalized candidate derivation, and both
repository and commit tie-breakers are explicitly covered. The accepted
production file hashes to
`73552e8cd8ccf259c4f6442688837dbc461d5e39170d297f084935ca34b03606`.
WP-034 may now perform one bounded combined live attempt.

The one authorized revision-11 combined attempt passed its offline preflight
but failed closed with only `PREPARATION_FAILED` and the package-runner failure
marker. No retry or subsequent provider access occurred. Independent post-run
verification is still 32/32 focused, 329/329 full preparation, and clean ops
TypeScript. The artifact, run report, dated success evidence, temporary files,
and backups are all absent, and `git diff --check` passes.

The public command intentionally discards the internal cause at the preparation
boundary. Absence of the run report proves only that failure occurred before or
during report writing; it cannot safely identify the provider or stage. WP-028
and WP-034 are therefore blocked. A new provider read for value-free stage
classification requires explicit authorization; no success, composition,
artifact-hash, or readiness claim is made.

On 2026-08-29 the Don explicitly authorized exactly one bounded diagnostic
provider flow. It may report only the first failing stage and a safe error code,
must stop on first failure, cannot run the full publication command or write an
artifact, report, or success evidence, and cannot expose provider values,
identities, URLs, timestamps, excerpts, headers, credentials, or contact data.

That sole diagnostic flow completed with zero retries. Stack preflight passed,
then GitHub Search stopped at the bounded transport with safe classification
`GITHUB_SEARCH|TransportError|UNSUPPORTED_STATUS`. Production maps any HTTP
response outside its accepted success range to that code before reading its
body. The diagnostic does not establish why GitHub returned that status, so it
does not authorize or justify a code fix.

The independent post-run sweep found the canonical artifact and run report
absent, zero matching temporary files, an unchanged 12-file evidence digest,
and an unchanged whole-workspace digest. One bootstrap-only ESM import-shape
attempt printed a local stack trace in the Worker's private transcript before
provider access; it disclosed no provider or credential value and changed no
file, but it is recorded as a procedural redaction-boundary deviation. WP-028
and WP-034 remain blocked.
