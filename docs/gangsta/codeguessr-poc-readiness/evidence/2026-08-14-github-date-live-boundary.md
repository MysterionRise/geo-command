---
heist: codeguessr-poc-readiness
evidence: github-date-live-boundary
observed-at: 2026-08-14T16:26:02Z
credential-material-recorded: false
artifact-published: false
run-report-published: false
---

# GitHub Committer-Date Live Boundary

The signed revision-10 combined preparation command failed closed after about
1.9 seconds with only `PREPARATION_FAILED` and the package-runner lifecycle
failure. No generated artifact, replaceable run report, success evidence,
temporary publication file, backup file, dataset output, blob output, or source
content was created.

Read-only stage isolation established that the signed Stack release, revision,
gate, access, terms, and account-holder acknowledgement preflight completes.
The earliest failing stage is the first exact GitHub commit-search query, which
returns `GITHUB_SEARCH_REJECTED` before an accepted query classification can be
published.

One explicitly authorized bounded aggregate inspection of that first query
reported five items, provider-reported incomplete state, one expected page, and
exact five-item cardinality. Required keys, repository-name shape, full commit
shape, URL-template consistency, and duplicate checks had zero failures.
Exactly one item used a parseable numeric-offset committer timestamp that does
not satisfy the signed adapter's lexical seconds-or-milliseconds UTC `Z`
canonicalization rule.

No timestamp value, response body, query text or URL, repository or commit
identity, source excerpt, header, credential, token, account value, or contact
value was logged or preserved. Provider access stopped after the bounded
diagnostics.

The operative implementation remains Green at 323/323 preparation tests with a
clean ops TypeScript check and no removed Stack SHA. A successful combined live
smoke now requires either a new Contract revision authorizing a deterministic
numeric-offset normalization rule with TDD evidence, or a later provider result
set that satisfies the current signed rule. No code fix is authorized by
Contract revision 10.
